describe("Data Type", () => {
  it("must declare", () => {
    const name: string = "Choirul Adamm";
    const balance: number = 10000;
    const isVip: boolean = false;

    console.info(name);
    console.info(balance);
    console.info(isVip);
  });
});
